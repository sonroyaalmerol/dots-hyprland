# NOTE: hypridle is replaced by snry-idle (built into snry-daemon).
# This config is kept as a reference only and is no longer loaded.
# To re-enable hypridle, revert execs.conf to use hypridle and uncomment below.

# $lock_cmd = hyprctl dispatch global quickshell:lock & pidof qs quickshell hyprlock || hyprlock
# # $lock_cmd = pidof hyprlock || hyprlock
# $suspend_cmd = systemctl suspend || loginctl suspend
# $lock_session = loginctl lock-session
#
# general {
#     lock_cmd = $lock_cmd
#     before_sleep_cmd = loginctl lock-session
#     after_sleep_cmd = hyprctl dispatch global quickshell:lockFocus
#     on_unlock_cmd = xrandr --output DP-1 --primary
#     inhibit_sleep = 3
# }
#
# # Normal idle: screen off at 5 min
# listener {
#     timeout = 300
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }
#
# # Normal idle: lock 5s after screen off
# # Also force dpms off to prevent lock screen from waking the display
# listener {
#     timeout = 305
#     on-timeout = loginctl lock-session && hyprctl dispatch dpms off
# }
